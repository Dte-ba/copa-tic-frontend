import Ember from "ember";

export default Ember.Route.extend({
  // requiere: "2",
  // model() {
  //   return {
  //     fase2: this.store.findRecord("fase", 3)
  //   };
  // },
  // afterModel(model) {
  //   var fase = this.store.findRecord("fase", 3);
  //
  //   console.log(fase.get("estado"));
  // }
});
